/**
 * userMutations — TanStack Query mutations for user profile updates.
 * FUTURE: replace AuthContext's updateUser with these.
 */
// import { useMutation, useQueryClient } from '@tanstack/react-query';
// import { updateUserInFirestore } from '../firebase/users';
// import { User } from '../../types/user';
//
// export function useUpdateUserMutation() {
//   const qc = useQueryClient();
//   return useMutation({
//     mutationFn: (data: Partial<User>) => updateUserInFirestore(data),
//     onSuccess: () => qc.invalidateQueries({ queryKey: ['user'] }),
//   });
// }

export {}; // placeholder
