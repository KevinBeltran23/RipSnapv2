/**
 * queryClient — TanStack Query client factory.
 * FUTURE: integrate when native rebuild is acceptable.
 */
// import { QueryClient } from '@tanstack/react-query';
// export const queryClient = new QueryClient({ defaultOptions: { queries: { staleTime: 5 * 60 * 1000 } } });

/** @placeholder — TanStack Query not yet installed. */
export const queryClient = null;
