/**
 * locationQueries — TanStack Query hooks for location data.
 * FUTURE: replace LocationContext's manual fetch logic with these.
 */
// import { useQuery } from '@tanstack/react-query';
// import { getAllLocations, searchLocations } from '../firebase/locations';
//
// export function useLocationsQuery() {
//   return useQuery({ queryKey: ['locations'], queryFn: getAllLocations, staleTime: 5 * 60 * 1000 });
// }
//
// export function useSearchQuery(query: string) {
//   return useQuery({ queryKey: ['locations', 'search', query], queryFn: () => searchLocations(query), enabled: query.length > 0 });
// }

export { }; // placeholder
